import { Theme } from "../core/Theme.slint";

export component OverflowScrollY inherits Rectangle {
    background: transparent;
    clip: true;

    Flickable {
        viewport-height: content.preferred-height;
        vertical-stretch: 1;

        content := VerticalLayout {
            spacing: Theme.sp-4;
            @children
        }
    }
}
