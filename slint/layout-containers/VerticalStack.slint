import { Theme } from "../core/Theme.slint";

export component VerticalStack inherits Rectangle {
    background: transparent;

    in property <length> gap: 8px;
    in property <string> alignment: "stretch";
    in property <length> stack-padding: 0px;

    VerticalLayout {
        spacing: gap;
        padding: stack-padding;
        @children
    }
}
