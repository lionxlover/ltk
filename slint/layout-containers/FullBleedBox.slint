import { Theme } from "../core/Theme.slint";

export component FullBleedBox inherits Rectangle {
    background: transparent;

    in property <length> bleed: 24px;

    Rectangle {
        x: -root.bleed;
        width: parent.width + (root.bleed * 2);
        height: parent.height;
    }
}
