import { Theme } from "../core/Theme.slint";

export component FullWidthSection inherits Rectangle {
    background: transparent;

    in property <int> padding-y: 48;
    in property <int> padding-x: 0;
}
