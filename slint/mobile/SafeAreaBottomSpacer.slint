import { Theme } from "../core/Theme.slint";

export component SafeAreaBottomSpacer inherits Rectangle {
    background: transparent;
    in property <length> home-bar-height: 34px;
    height: home-bar-height;
}
