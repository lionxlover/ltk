import { Theme } from "../core/Theme.slint";

export component ReachabilityHelper inherits Rectangle {
    background: transparent;

    in property <bool> active: false;
    in property <length> shift-down: 120px;

    y: active ? shift-down : 0px;
}
