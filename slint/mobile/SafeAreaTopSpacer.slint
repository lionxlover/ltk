import { Theme } from "../core/Theme.slint";

export component SafeAreaTopSpacer inherits Rectangle {
    background: transparent;
    in property <length> notch-height: 44px;
    height: notch-height;
}
