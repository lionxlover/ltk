import { Theme } from "../core/Theme.slint";

export component HeadingH1 inherits Text {
    text: "Heading H1";
    color: Theme.text-primary;
    font-family: Theme.font-ui;
    font-size: 32px;
    font-weight: Theme.weight-bold;
    letter-spacing: -0.5px;
}
