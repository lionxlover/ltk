import { Theme } from "../core/Theme.slint";

export component BodyLG inherits Text {
    text: "Body Large — comfortable reading size for longer content.";
    color: Theme.text-primary;
    font-family: Theme.font-ui;
    font-size: 16px;
    font-weight: Theme.weight-regular;
    wrap: word-wrap;
}
