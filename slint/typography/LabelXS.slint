import { Theme } from "../core/Theme.slint";

export component LabelXS inherits Text {
    text: "LABEL XS";
    color: Theme.text-tertiary;
    font-family: Theme.font-ui;
    font-size: 10px;
    font-weight: Theme.weight-semibold;
    letter-spacing: 0.5px;
}
