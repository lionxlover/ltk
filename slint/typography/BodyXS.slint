import { Theme } from "../core/Theme.slint";

export component BodyXS inherits Text {
    text: "Body XS — minimum readable text, tooltips, helper copy.";
    color: Theme.text-tertiary;
    font-family: Theme.font-ui;
    font-size: 11px;
    font-weight: Theme.weight-regular;
    wrap: word-wrap;
}
