import { Theme } from "../core/Theme.slint";

export component LabelSM inherits Text {
    text: "Label Small";
    color: Theme.text-secondary;
    font-family: Theme.font-ui;
    font-size: 12px;
    font-weight: Theme.weight-medium;
}
