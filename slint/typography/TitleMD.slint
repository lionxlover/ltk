import { Theme } from "../core/Theme.slint";

export component TitleMD inherits Text {
    text: "Title Medium";
    color: Theme.text-primary;
    font-family: Theme.font-ui;
    font-size: 18px;
    font-weight: Theme.weight-medium;
}
