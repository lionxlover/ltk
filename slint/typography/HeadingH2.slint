import { Theme } from "../core/Theme.slint";

export component HeadingH2 inherits Text {
    text: "Heading H2";
    color: Theme.text-primary;
    font-family: Theme.font-ui;
    font-size: 28px;
    font-weight: Theme.weight-bold;
    letter-spacing: -0.25px;
}
