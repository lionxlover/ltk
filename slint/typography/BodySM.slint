import { Theme } from "../core/Theme.slint";

export component BodySM inherits Text {
    text: "Body Small — secondary content, descriptions, supporting text.";
    color: Theme.text-secondary;
    font-family: Theme.font-ui;
    font-size: 12px;
    font-weight: Theme.weight-regular;
    wrap: word-wrap;
}
