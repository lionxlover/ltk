import { Theme } from "../core/Theme.slint";

export component AriaLivePolite inherits Rectangle {
    background: transparent;

    in property <string> message: "";
    in property <bool> atomic: true;
}
