import { Theme } from "../core/Theme.slint";

export component AriaAnnouncer inherits Rectangle {
    background: transparent;

    in property <string> message: "";
    in property <bool> assertive: false;
}
