import { Theme } from "../core/Theme.slint";

export component RovingTabIndexGroup inherits Rectangle {
    background: transparent;

    in property <int> item-count: 4;
    in-out property <int> active-index: 0;
    in property <bool> wrap: true;
}
