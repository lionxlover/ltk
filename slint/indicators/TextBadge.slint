import { Theme } from "../core/Theme.slint";

export component TextBadge inherits Rectangle {
    in property <string> label: "";
    in property <int> variant: 0;

    min-width: 24px;
    height: 22px;
    background: variant == 1 ? Theme.accent-subtle : (variant == 2 ? Theme.green-100 : (variant == 3 ? Theme.amber-100 : (variant == 4 ? Theme.red-100 : Theme.bg-overlay)));
    border-radius: Theme.radius-sm;
    horizontal-stretch: 0;

    label-text := Text {
        x: (parent.width - self.width) / 2;
        y: (parent.height - self.height) / 2;
        text: root.label;
        color: variant == 1 ? Theme.accent : (variant == 2 ? Theme.green-700 : (variant == 3 ? Theme.amber-600 : (variant == 4 ? Theme.red-600 : Theme.text-secondary)));
        font-family: Theme.font-ui;
        font-size: Theme.text-xs;
        font-weight: Theme.weight-semibold;
        vertical-alignment: center;
        horizontal-alignment: center;
    }
}
