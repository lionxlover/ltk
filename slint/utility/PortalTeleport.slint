import { Theme } from "../core/Theme.slint";

export component PortalTeleport2 inherits Rectangle {
    background: transparent;

    in property <bool> active: true;
    in property <length> target-x: 0;
    in property <length> target-y: 0;
}
