import { Theme } from "../core/Theme.slint";

export component MaskedInput inherits Rectangle {
    in-out property <string> text;
    in-out property <string> mask: "(###) ###-####";
    in-out property <string> placeholder;
    in-out property <bool> has-focus <=> input.has-focus;

    background: Theme.bg-surface;
    border-radius: Theme.radius-md;
    border-width: 1px;
    border-color: has-focus ? Theme.accent : Theme.border-base;
    height: 40px;
    horizontal-stretch: 1;

    TouchArea {
        clicked => { input.focus(); }
    }

    if root.text == "" && !input.has-focus: Text {
        x: 12px;
        y: (parent.height - self.height) / 2;
        text: root.placeholder == "" ? root.mask : root.placeholder;
        color: Theme.text-placeholder;
        font-family: Theme.font-mono;
        font-size: 14px;
        vertical-alignment: center;
    }

    input := TextInput {
        x: 12px;
        y: (parent.height - self.height) / 2;
        width: parent.width - 24px;
        text <=> root.text;
        color: Theme.text-primary;
        font-family: Theme.font-mono;
        font-size: 14px;
        vertical-alignment: center;
    }
}
