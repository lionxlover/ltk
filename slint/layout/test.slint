import { Theme } from "../core/Theme.slint";
import { Spacer } from "Spacer.slint";
import { Grid } from "Grid.slint";
import { RoundedRect } from "RoundedRect.slint";

export component LayoutTest inherits Rectangle {
    background: Theme.bg-base;
    width: 800px;
    height: 700px;

    Flickable {
        width: parent.width;
        height: parent.height;
        viewport-height: content.preferred-height;

        content := VerticalLayout {
            padding: Theme.sp-6;
            spacing: Theme.sp-6;

            Text {
                text: "Layout — Test";
            color: Theme.text-primary;
            font-family: Theme.font-display;
            font-size: Theme.text-3xl;
            font-weight: Theme.weight-bold;
        }

        Text { text: "Spacer (horizontal)"; color: Theme.text-secondary; font-size: Theme.text-sm; font-weight: Theme.weight-semibold; }
        Rectangle {
            height: 40px;
            background: Theme.bg-surface;
            border-radius: Theme.radius-md;
            border-width: 1px;
            border-color: Theme.border-subtle;

            HorizontalLayout {
                padding: Theme.sp-2;

                Rectangle { width: 60px; height: 32px; border-radius: Theme.radius-sm; background: Theme.accent; }
                Spacer { horizontal: true; }
                Rectangle { width: 60px; height: 32px; border-radius: Theme.radius-sm; background: Theme.state-success; }
            }
        }

        Text { text: "Spacer (vertical)"; color: Theme.text-secondary; font-size: Theme.text-sm; font-weight: Theme.weight-semibold; }
        Rectangle {
            height: 120px;
            background: Theme.bg-surface;
            border-radius: Theme.radius-md;
            border-width: 1px;
            border-color: Theme.border-subtle;

            VerticalLayout {
                padding: Theme.sp-2;

                Rectangle { height: 24px; border-radius: Theme.radius-sm; background: Theme.state-warning; horizontal-stretch: 1; }
                Spacer { }
                Rectangle { height: 24px; border-radius: Theme.radius-sm; background: Theme.state-error; horizontal-stretch: 1; }
            }
        }

        Text { text: "Grid"; color: Theme.text-secondary; font-size: Theme.text-sm; font-weight: Theme.weight-semibold; }
        Grid {
            height: 120px;
            gap: Theme.sp-3;

            Rectangle { row: 0; col: 0; background: Theme.accent; border-radius: Theme.radius-sm; }
            Rectangle { row: 0; col: 1; background: Theme.state-success; border-radius: Theme.radius-sm; }
            Rectangle { row: 1; col: 0; background: Theme.state-warning; border-radius: Theme.radius-sm; }
            Rectangle { row: 1; col: 1; background: Theme.state-error; border-radius: Theme.radius-sm; }
        }

        Text { text: "RoundedRect"; color: Theme.text-secondary; font-size: Theme.text-sm; font-weight: Theme.weight-semibold; }
        HorizontalLayout { spacing: Theme.sp-3;
            RoundedRect { width: 100px; height: 60px; root-color: Theme.bg-surface; }
            RoundedRect { width: 100px; height: 60px; root-color: Theme.accent; root-radius: Theme.radius-xl; }
            RoundedRect { width: 100px; height: 60px; root-color: Theme.bg-raised; border-enabled: false; }
        }
        }
    }
}
