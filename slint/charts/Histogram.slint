import { Theme } from "../core/Theme.slint";

export component Histogram inherits Rectangle {
    in property <[int]> bins: [3, 7, 12, 9, 5, 2];
    in property <string> label: "Histogram";

    background: Theme.bg-surface;
    border-radius: Theme.radius-md;
    border-width: 1px;
    border-color: Theme.border-subtle;

    VerticalLayout {
        padding: Theme.sp-3;
        spacing: Theme.sp-2;

        Text { text: root.label; color: Theme.text-secondary; font-size: Theme.text-xs; font-weight: Theme.weight-semibold; }

        // Same bug as BarChartVertical: `alignment: end` on this outer
        // HorizontalLayout overrode `horizontal-stretch`, collapsing
        // every bin to 0px wide. Removed — the inner VerticalLayout's own
        // `alignment: end` (bottom-aligning each bin's single bar) was
        // always fine and needed no change.
        HorizontalLayout { vertical-stretch: 1; spacing: 2px;
            for bin[i] in root.bins: Rectangle { horizontal-stretch: 1;
                VerticalLayout { alignment: end;
                    Rectangle { height: bin * 6px; background: Theme.accent; border-radius: 2px; }
                }
            }
        }
    }
}
