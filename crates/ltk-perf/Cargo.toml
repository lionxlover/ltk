[package]
name        = "ltk-perf"
description = "LTK performance: profiler, frame analyzer, memory analyzer, optimizer"
version.workspace = true
edition.workspace = true
license.workspace = true
[dependencies]
ltk-core = { path = "../ltk-core" }
parking_lot = { workspace = true }
log         = { workspace = true }
