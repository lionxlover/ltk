[package]
name        = "ltk-security"
description = "LTK security: capability permissions, sandbox interface, secure clipboard"
version.workspace = true
edition.workspace = true
license.workspace = true
[dependencies]
ltk-core = { path = "../ltk-core" }
log      = { workspace = true }
