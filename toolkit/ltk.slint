// ============================================================================
//  LTK — Lion Tool Kit  v2.0
//  Designed by Lion · Inspired by macOS HIG + GNOME HIG 3
//  Built for LionOS · © 2026 Lion
//
//  Single-file import:
//    import { LTKPalette, LTKPrimaryButton, LTKAppWindow } from "ltk/ltk.slint";
// ============================================================================

// ── Theme ─────────────────────────────────────────────────────────────────────
import { LTKPalette }   from "theme/palette.slint";
import { LTKType }      from "theme/typography.slint";
import { LTKSpace }     from "theme/spacing.slint";
import { LTKRadius }    from "theme/radius.slint";
import { LTKMotion }    from "theme/motion.slint";
import { LTKShadow }    from "theme/shadows.slint";
import { LTKIcons }     from "theme/icons.slint";

export { LTKPalette, LTKType, LTKSpace, LTKRadius, LTKMotion, LTKShadow, LTKIcons }

// ── Windows ───────────────────────────────────────────────────────────────────
import { LTKTrafficLights }    from "windows/LTKTrafficLights.slint";
import { LTKHeaderBar }        from "windows/LTKHeaderBar.slint";
import { LTKAppWindow }        from "windows/LTKAppWindow.slint";
import { LTKDialogWindow }     from "windows/LTKDialogWindow.slint";

export { LTKTrafficLights, LTKHeaderBar, LTKAppWindow, LTKDialogWindow }

// ── Buttons ───────────────────────────────────────────────────────────────────
import { LTKButton }            from "buttons/LTKButton.slint";
import { LTKPrimaryButton }     from "buttons/LTKPrimaryButton.slint";
import { LTKSecondaryButton }   from "buttons/LTKSecondaryButton.slint";
import { LTKDestructiveButton } from "buttons/LTKDestructiveButton.slint";
import { LTKGhostButton }       from "buttons/LTKGhostButton.slint";
import { LTKIconButton }        from "buttons/LTKIconButton.slint";
import { LTKFAButton }          from "buttons/LTKFAButton.slint";
import { LTKSegmentControl, LTKSegment } from "buttons/LTKSegmentControl.slint";
import { LTKToggleSwitch }      from "buttons/LTKToggleSwitch.slint";
import { LTKChip }              from "buttons/LTKChip.slint";

export { LTKButton, LTKPrimaryButton, LTKSecondaryButton, LTKDestructiveButton,
         LTKGhostButton, LTKIconButton, LTKFAButton,
         LTKSegmentControl, LTKSegment, LTKToggleSwitch, LTKChip }

// ── Inputs ────────────────────────────────────────────────────────────────────
import { LTKTextField }   from "inputs/LTKTextField.slint";
import { LTKPasswordField}from "inputs/LTKPasswordField.slint";
import { LTKSearchField } from "inputs/LTKSearchField.slint";
import { LTKTextArea }    from "inputs/LTKTextArea.slint";
import { LTKNumberField } from "inputs/LTKNumberField.slint";
import { LTKCheckBox }    from "inputs/LTKCheckBox.slint";
import { LTKRadioButton } from "inputs/LTKRadioButton.slint";
import { LTKSlider }      from "inputs/LTKSlider.slint";
import { LTKRangeSlider } from "inputs/LTKRangeSlider.slint";
import { LTKComboBox, LTKOption } from "inputs/LTKComboBox.slint";
import { LTKOTPField }    from "inputs/LTKOTPField.slint";

export { LTKTextField, LTKPasswordField, LTKSearchField, LTKTextArea, LTKNumberField,
         LTKCheckBox, LTKRadioButton, LTKSlider, LTKRangeSlider, LTKComboBox,
         LTKOption, LTKOTPField }

// ── Navigation ────────────────────────────────────────────────────────────────
import { LTKSidebar, LTKNavEntry }        from "navigation/LTKSidebar.slint";
import { LTKTabBar, LTKTab, LTKTabStyle } from "navigation/LTKTabBar.slint";
import { LTKBreadcrumb, LTKCrumb }        from "navigation/LTKBreadcrumb.slint";
import { LTKStatusBar }                   from "navigation/LTKStatusBar.slint";
import { LTKToolbar, LTKToolbarSeparator, LTKToolbarSpacer } from "navigation/LTKToolbar.slint";
import { LTKWindowSwitcher, LTKWindowThumb } from "navigation/LTKWindowSwitcher.slint";

export { LTKSidebar, LTKNavEntry, LTKTabBar, LTKTab, LTKTabStyle,
         LTKBreadcrumb, LTKCrumb, LTKStatusBar,
         LTKToolbar, LTKToolbarSeparator, LTKToolbarSpacer,
         LTKWindowSwitcher, LTKWindowThumb }

// ── Containers ────────────────────────────────────────────────────────────────
import { LTKCard, LTKCardMaterial }       from "containers/LTKCard.slint";
import { LTKGlassPanel, LTKGlassTier }   from "containers/LTKGlassPanel.slint";
import { LTKPopover }                     from "containers/LTKPopover.slint";
import { LTKAccordionItem }               from "containers/LTKAccordion.slint";
import { LTKSectionGroup, LTKSectionRow } from "containers/LTKSection.slint";
import { LTKProgressBar }                 from "containers/LTKProgressBar.slint";
import { LTKSpinner }                     from "containers/LTKSpinner.slint";
import { LTKBadge }                       from "containers/LTKBadge.slint";
import { LTKAvatar, LTKAvatarShape }      from "containers/LTKAvatar.slint";
import { LTKDivider }                     from "containers/LTKDivider.slint";
import { LTKSkeleton, LTKSkeletonCard }   from "containers/LTKSkeleton.slint";
import { LTKEmptyState }                  from "containers/LTKEmptyState.slint";
import { LTKMenuRow, LTKMenuSeparator, LTKContextMenu } from "containers/LTKMenuRow.slint";

export { LTKCard, LTKCardMaterial, LTKGlassPanel, LTKGlassTier, LTKPopover,
         LTKAccordionItem, LTKSectionGroup, LTKSectionRow,
         LTKProgressBar, LTKSpinner, LTKBadge, LTKAvatar, LTKAvatarShape,
         LTKDivider, LTKSkeleton, LTKSkeletonCard, LTKEmptyState,
         LTKMenuRow, LTKMenuSeparator, LTKContextMenu }

// ── Notifications ─────────────────────────────────────────────────────────────
import { LTKToast, LTKToastKind, LTKToastPosition } from "notifications/LTKToast.slint";
import { LTKBanner, LTKBannerKind }                  from "notifications/LTKBanner.slint";
import { LTKTooltip }                                 from "notifications/LTKTooltip.slint";

export { LTKToast, LTKToastKind, LTKToastPosition,
         LTKBanner, LTKBannerKind, LTKTooltip }

// ── Layouts ───────────────────────────────────────────────────────────────────
import { LTKAdaptiveLayout } from "layouts/LTKAdaptiveLayout.slint";
import { LTKSplitView }      from "layouts/LTKSplitView.slint";

export { LTKAdaptiveLayout, LTKSplitView }

// ── Animations ────────────────────────────────────────────────────────────────
import { LTKAnimatedContainer } from "animations/LTKAnimatedContainer.slint";
import { LTKShimmer }           from "animations/LTKShimmer.slint";
import { LTKPulse }             from "animations/LTKPulse.slint";

export { LTKAnimatedContainer, LTKShimmer, LTKPulse }

// ── Editor ────────────────────────────────────────────────────────────────────
import { LTKCodeEditor }    from "editor/LTKCodeEditor.slint";
import { LTKTerminalView }  from "editor/LTKTerminalView.slint";

export { LTKCodeEditor, LTKTerminalView }

// ── Advanced / Data / Charts ─────────────────────────────────────────────────
import { LTKListView, LTKListItem }         from "advanced/LTKListView.slint";
import { LTKTableView, LTKColumn, LTKTableRow } from "advanced/LTKTableView.slint";
import { LTKLineChart, LTKSeries, LTKDataPoint } from "advanced/LTKLineChart.slint";
import { LTKBarChart, LTKBarItem }          from "advanced/LTKBarChart.slint";
import { LTKDonutChart, LTKPieSlice }       from "advanced/LTKDonutChart.slint";
import { LTKGauge }                         from "advanced/LTKGauge.slint";
import { LTKChatPanel, LTKChatMessage }     from "advanced/LTKChatPanel.slint";
import { LTKSystemTray, LTKTrayItem }       from "advanced/LTKSystemTray.slint";
import { LTKPropertyInspector, LTKProperty }from "advanced/LTKPropertyInspector.slint";
import { LTKClockWidget, LTKBatteryWidget,
         LTKVolumeWidget, LTKNetworkWidget } from "advanced/LTKSystemWidgets.slint";

export { LTKListView, LTKListItem,
         LTKTableView, LTKColumn, LTKTableRow,
         LTKLineChart, LTKSeries, LTKDataPoint,
         LTKBarChart, LTKBarItem,
         LTKDonutChart, LTKPieSlice,
         LTKGauge,
         LTKChatPanel, LTKChatMessage,
         LTKSystemTray, LTKTrayItem,
         LTKPropertyInspector, LTKProperty,
         LTKClockWidget, LTKBatteryWidget,
         LTKVolumeWidget, LTKNetworkWidget }

// ── Media ─────────────────────────────────────────────────────────────────────
import { LTKImageViewer } from "media/LTKImageViewer.slint";
import { LTKVideoPlayer } from "media/LTKVideoPlayer.slint";

export { LTKImageViewer, LTKVideoPlayer }

// ── Dialogs ───────────────────────────────────────────────────────────────────
import { LTKAlertDialog, LTKAlertKind } from "dialogs/LTKAlertDialog.slint";
import { LTKColorPicker }               from "dialogs/LTKColorPicker.slint";

export { LTKAlertDialog, LTKAlertKind, LTKColorPicker }

// ── Additional Advanced Components ────────────────────────────────────────────
import { LTKCalendar }                              from "advanced/LTKCalendar.slint";
import { LTKFileChooser, LTKFileItem }              from "advanced/LTKFileChooser.slint";
import { LTKNotificationCenter, LTKNotification }   from "advanced/LTKNotificationCenter.slint";
import { LTKCommandPalette, LTKCommand }             from "advanced/LTKCommandPalette.slint";
import { LTKSettingsPage, LTKSettingsSection }       from "advanced/LTKSettingsPage.slint";
import { LTKOnboarding, LTKOnboardingStep }          from "advanced/LTKOnboarding.slint";
import { LTKKanban, LTKKanbanColumn, LTKKanbanCard } from "advanced/LTKKanban.slint";

export { LTKCalendar,
         LTKFileChooser, LTKFileItem,
         LTKNotificationCenter, LTKNotification,
         LTKCommandPalette, LTKCommand,
         LTKSettingsPage, LTKSettingsSection,
         LTKOnboarding, LTKOnboardingStep,
         LTKKanban, LTKKanbanColumn, LTKKanbanCard }

// ── Additional Media ──────────────────────────────────────────────────────────
import { LTKMediaGallery, LTKMediaItem } from "media/LTKMediaGallery.slint";

export { LTKMediaGallery, LTKMediaItem }

// ── Final Components ──────────────────────────────────────────────────────────
import { LTKControlCenter }               from "advanced/LTKControlCenter.slint";
import { LTKTaskManager, LTKProcess }     from "advanced/LTKTaskManager.slint";
import { LTKTimeline, LTKTimelineEvent }  from "advanced/LTKTimeline.slint";
import { LTKMarkdownView, LTKMdBlock }    from "advanced/LTKMarkdownView.slint";
import { LeonuxRadius, LeonuxSpace }      from "theme/leonux_tokens.slint";

export { LTKControlCenter,
         LTKTaskManager, LTKProcess,
         LTKTimeline, LTKTimelineEvent,
         LTKMarkdownView, LTKMdBlock,
         LeonuxRadius, LeonuxSpace }
